# Cerebras가 사내 지식 베이스를 구축한 방법

- **원문 출처**: [x.com/cerebras](https://x.com/cerebras)
- **GeekNews**: https://news.hada.io/topic?id=31850
- **등록**: GN⁺ (@neo)

---

## 개요

Cerebras는 Slack, 코드 저장소, 문서, 내부 데이터베이스를 **기존 위치에서 직접 수집**하는
"Cerebras Knowledge"를 구축했다. 출시 3개월 만에 **하루 15,000건 이상의 질문**을
처리하게 되었다. (직원 + 자동화 + 에이전트의 질의 합계)

## 아키텍처의 핵심

- 모든 데이터를 하나의 도구로 옮기지 않고 **공통 스키마의 Postgres 임베딩 테이블에 연결**
  - 데이터는 원래 있던 자리에 그대로 둔다
- **수집 / 질의 / 인증·권한·감사 계층을 분리** → 새로운 데이터 소스 추가가 쉬워진다

## Slack 검색 기법

- 원문 임베딩만으로는 부족 → **전문 검색 + 임베딩 검색 + 역문서 빈도(IDF) + 시간 감쇠**를 함께 사용
  - 짧은 메시지와 긴 기술 토론이 섞여 있어 임베딩 단독으로는 품질이 안 나왔다
- **스레드 요약과 개별 발화를 별도로 임베딩** → 맥락과 디테일을 모두 커버
- 실시간 수집에는 **Socket Mode** 사용

## 코드 저장소 처리

- 40GB+ 규모의 대형 저장소를 다루기 위해 **CocoIndex** 프레임워크 선택
- **언어별 정규식 경계를 큰 단위 → 작은 단위로 계층적으로 적용**해 청킹

## 질의 처리

- **LLM이 먼저 사용할 검색 도구를 계획(query planning)** 하고 병렬로 수집
- **RRF(Reciprocal Rank Fusion)와 재순위화(reranking) 모델**로 결과 통합
- **MCP에서는 검색 기능을 원시 도구로 공개**
  - `search_slack`, `search_code`, `who_knows`
- 웹 UI는 **planner → executor → synthesizer** 파이프라인 전체를 오케스트레이션

## 조직 구조에 맞춘 검색 범위

- 코퍼스가 커지면서 관련도가 떨어지는 문제 발생
- Slack 채널, 저장소, 문서 공간을 팀 기준으로 묶은
  **"프로젝트 단위 검색 범위"를 기본값으로 설정**

---

## 댓글

- **menjkl**: "나이스 한데요 시행착오가 얼마나 많았을지가 보이네요."
- **hyeongjun**: "좋은 글 감사합니다"
- **wayden**: "Slack 대화를 어떻게 필터링하여 지식 베이스에 포함시키는지, 그 실구현체가 궁금하네요."

---

## 메모 — 내 상황에 적용해볼 점

- 데이터를 한곳으로 모으지 않고 **임베딩 테이블만 공통 스키마로 통일**하는 접근
- 하이브리드 검색(전문 + 임베딩 + IDF + 시간 감쇠)은 짧은 대화 데이터에 특히 유효
- MCP는 **원시 검색 도구만 노출**하고, 오케스트레이션은 호출하는 쪽이 담당하는 분리 구조
