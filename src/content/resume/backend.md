---
title: 경력기술서
summary: 어떤 문제를 맡았고, 무엇을 선택했고, 무엇이 달라졌는지.
updated: 2026-09-23

# 요약 — 문서 맨 앞 세 줄. "무엇을 하는 개발자인지"가 먼저 읽히게.
intro:
  - 센서와 단말에서 올라오는 데이터를 수집·저장하고, 그 위에서 이상을 찾아내는 백엔드를 만들어 왔습니다.
  - Spark·Hive로 대용량 주행 데이터를 다루고, 장치 20여 종의 특징 추출과 이상 탐지 모델을 하나의 파이프라인으로 묶었습니다.
  - 장치나 도메인이 늘어도 클래스만 추가하면 붙는 구조를 선호하고, 수집·진단 과정을 지표로 남겨 추적할 수 있게 만듭니다.

# 핵심 역량 — 묶음 하나가 카드 한 장, 항목 하나가 작은 칩 하나로 그려집니다.
skills:
  - label: Language
    items: [Java, C#, Python]
  - label: Data / ML
    items: [Spark, Hive, Hadoop, PyTorch, scikit-learn]
  - label: Framework
    items: [Spring Boot, MyBatis, .NET WinForms]
  - label: Database
    items: [MySQL]
  - label: Infra
    items: [AWS, Docker, GitHub Actions, OpenTelemetry]

# 경력 — 프로젝트 하나가 가로로 긴 카드 한 장.
# points 는 항상 보이고, detail 은 카드에 마우스를 올리면 펼쳐집니다.
career:
  - role: Backend · Data Engineer
    period: 2023.09 – 재직 중
    projects:
      - name: 센서 데이터 이상 탐지 머신러닝 파이프라인 구축
        stack: [Python, Spark, PyTorch, scikit-learn]
        points:
          - 장치 센서 데이터에서 특징을 추출해 학습·추론하는 이상 탐지 파이프라인을 코어 개발
          - 학습과 추론을 비동기 작업으로 처리하고, 진행 상태와 결과 이력을 화면에서 확인하도록 구성
        detail:
          - 20여 종 장치의 특징 추출기를 등록 구조로 관리하고, 장치 간 시점 정렬 결합 로직을 공통화
          - VAE·PCA·Isolation Forest·One-Class SVM을 같은 규격으로 두어 모델을 바꿔 끼울 수 있게 구성
          - 하이퍼파라미터와 임계 민감도를 설정으로 분리해 재학습 시 자동 적용
          - Spark 실행 옵션을 조정해 소량 데이터가 과도하게 분할되던 문제를 완화
      - name: 전동차 고장 진단 시스템 개발
        stack: [Python, Java, Spring Boot, Spark, Hive]
        points:
          - 전동차 18종 장치의 주행 센서 데이터를 분석해 이상을 판정하는 고장 진단 알고리즘 개발
          - 장치별 진단 로직을 같은 구조로 표준화하고, 조회 쿼리에 파티션 조건을 적용해 대용량 조회 부담 완화
        detail:
          - 쌓여 있는 센서 데이터를 Spark로 읽어 장치별 임계치와 비교하고, 검지·조치 코드와 함께 이력으로 적재
          - 장치를 등록만 하면 연결되는 구조로 설계해, 장치가 늘어도 기존 코드를 고치지 않도록 함
          - 진단 소요 시간과 실패 내역을 공통 모듈에서 자동으로 수집해 느린 장치를 추적
          - Python으로 만든 진단 로직을 Java·Spring 기반으로 이식하며 반복 계산을 공통 모듈로 분리
      - name: 실시간 열차 데이터 수집 게이트웨이 개발
        stack: [Java, Spring Cloud Gateway, Netty]
        points:
          - 주행 중인 전동차가 보내는 실시간 데이터를 수신·해석해 내부 서버로 전달하는 게이트웨이 개발
          - 바이너리 패킷의 항목 위치를 상수로 정리해 장치별 데이터 추출 로직을 표준화
        detail:
          - 요청 경로별 라우팅, 웹소켓 전용 필터, 수집 서버로의 UDP 전달 구성
          - 로그 처리 단계가 패킷 버퍼를 읽어 소진하는 바람에 다음 단계에 빈 데이터가 전달되던 장애를 원인 분석 후 수정
          - 편성별 로그를 남겨 어느 열차의 데이터인지 추적할 수 있도록 개선
      - name: 에러 모니터링 서버 신규 구축
        stack: [Java, Spring Boot, OpenTelemetry, Slack API]
        points:
          - 로그 수집 도구를 사내 공통 모듈로 배포해 코드 수정 없이 MSA 10여 개 서비스의 에러를 자동 수집
          - 분산 추적을 도입해 서비스 간 호출을 하나의 흐름으로 잇고, 발생 즉시 담당자에게 Slack 알림
        detail:
          - 수집 서버를 설계부터 배포까지 코어 개발로 맡아 새로 구축
          - 도입 전에는 이상을 감지한 뒤 컨테이너 로그를 직접 열어 찾아야 했지만, 이후 에러가 먼저 담당자에게 도착
          - OpenTelemetry로 서비스 간 호출에 추적 ID를 전파해, 어느 서비스에서 시작된 요청인지 한 줄로 추적
          - 에러가 발생한 위치와 요청 정보를 함께 담아 보내, 재현 없이 원인을 좁힐 수 있게 함
          - 알림을 도메인별로 나누는 구조를 잡아, 새 서비스가 늘어도 클래스 하나만 추가하면 되도록 설계
          - 에러 종류에 따라 담당자를 자동으로 지목하고 알림 채널을 분리
      - name: POS 클라이언트 자동 업데이트 프로그램 개발
        stack: [C#, .NET, WinForms]
        points:
          - 매장 단말의 프로그램 버전을 서버와 비교해 내려받아 교체하는 자동 업데이트 프로그램을 코어 개발
          - 실행 중 교체가 불가능한 업데이터 자신은 별도 프로그램이 교체 후 재실행하는 방식으로 무인 갱신 처리
        detail:
          - 브랜드·기기 종류(POS·키오스크·QR·에이전트·배달중계)에 따라 화면과 설치 경로, 갱신 대상을 분기
          - 관리자 권한 자동 승격, 중복 실행 차단, 결제 게이트웨이 종료 후 갱신으로 현장 설치 실패 요인 제거
          - 최초 설치는 운영사 계정, 이후에는 단말 라이선스로 인증하는 2단 구조와 갱신 이력 서버 전송
---
